name: Build and push image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    # Self-hosted containerized runners on the homelab. Migrated from
    # ubuntu-latest 2026-05-13 after the monthly $2 hosted-runner cap
    # was exhausted. See docs/decisions/self-hosted-runners-migration.md.
    # Two replicas register under the `poob` label set so this workflow
    # does not contend with permitscope's runners (which use `permitscope`).
    runs-on: [self-hosted, linux, x64, poob]
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          build-args: |
            GIT_SHA=${{ github.sha }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/poob:latest
            ghcr.io/${{ github.repository_owner }}/poob:${{ github.sha }}
          # Registry cache is persistent across builds and not subject to
          # GHA cache's 10 GB / 7-day eviction policy. GHA cache is kept as
          # a fallback for the first build on a branch without a warm
          # registry cache yet.
          cache-from: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/poob:buildcache
            type=gha
          cache-to: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/poob:buildcache,mode=max
            type=gha,mode=max
